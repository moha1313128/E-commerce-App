import 'package:flutter/material.dart';

void main(){
  runApp(
    new MaterialApp(
      debugShowCheckedModeBanner: false,
      home: new Container(
        color: Colors.white,
        child: new Center(
          child: new Text('WebDevMa'),
        )
      )
    )
  );
}